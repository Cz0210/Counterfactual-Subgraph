# Main / L0 / Mut / T13 / T14 closeout — 2026-09-06

## Confirmed independent results

Main remains12/16. Missing: Mutagenicity/ComRecGC and TasteMolNet's
GCFExplainer, GlobalGCE, ComRecGC. The original AutoDL matrix authority and
publishers are unchanged. GNN and LLM registries do not contribute cells.

Corrected GNN seed7 remains independently accepted and published. No training,
temperature fit, GNN OT, candidate generation or original transfer was repeated.
Small paper tables are at
`/private/tmp/t13-gnn-llm-closeout-20260906/paper-gnn-corrected`.

L0 evaluation2567257 and package2567271 both COMPLETED0:0. New science reused
the original386×8 attempts,472-vocabulary and15-rule eligible universe from
failed2560839. Historical FAILED stays unchanged. Raw distinct fragments19 and
eligible rules15 are different counts; proposal_shortfall is0, not5.
At-most-K applies to all LLM variants, not main/GNN: K16–20 use the same15 rules,
no padding/new attempts. Calibration66 was globally frozen before test141.
Both cohorts are the main all-true-source-label1 definition, not GNN native/common.
CCRCov@10=@20=0.19858156028368795; conditional median WNode at10 is
0.004638647893874328. The136 first L0 distance cache misses are new L0 work,
not recomputation of GNN/compliant prior OT.

L0 AutoDL result:
`/autodl-fs/data/counterfactual-subgraph-runtime/outputs/autodl/ablations/llm/bace-l0-at-most-k-c6d5f0b5-20260906/import-68TBki/result`.
Its sibling publication/import receipts prove scoped HPC→Mac→AutoDL transfer.
Science commit c6d5f0b5, import driver3c70af72 are separately recorded.
LLM registry: `outputs/autodl/ablations/llm/registry/core-at-most-k-20260906`.
Local audited tables and derived partial TeX are in
`/private/tmp/l0-mut-t13-closeout-20260906/l0-paper`.

## Mut: resource stop, not scientific divergence

One bounded read of the frozen observer proves1-based events1..249, while
the complete checkpoint records250/next251. Debug move_index249 is0-based,
but lacks the complete common observer fields. No positive joint committed
checkpoint exists. Do not fabricate event250 or start at251. The only evidenced
route is fresh deterministic replay1..250, compare against old250 scientific
state, then continue the same A arm and sequential B, with independent reload.
The previous250 steps took about36hours; this is not a promise for new runtime.

The actual observer→checkpoint joint-boundary implementation is tested and
integrated (`c67af7d7`, original isolated591aeab8). It flushes/fsyncs the observed
event before the existing checkpoint/storage callback, then atomically seals
the matching event/checkpoint receipt. No new RNG or scientific action changes.
Old missing-event roots cannot be resumed by pretending they have joint250.

No Mut recovery science has started. Both old A owner193161 and post-AB193450
are absent; executor222378 is genuinely alive waiting. In addition to inode
admission, a fresh A/B spec plus matching post-AB/executor bindings remain to be
sealed. ENGINEERING_REPAIR is not an automatic A/B retry command. Do not claim
unattended Mut completion or trigger RouteB from inode failure.

The existing executable entrypoints are present, but the live222378 executor
loads its spec only at startup and exposes no reload/rebind CLI. Its immutable
input remains `control/mut_next_stage_executor_ebada9c0_20260904T195542Z/spec-bundle/task-spec.json`.
The old A/B spec is `control/mut_same_contract_ab_task_spec_20260904T052257Z_7c9a0159.json`;
the old post-AB root is `control/mut_post_ab_continuation_v1_20260904T052257Z_7c9a0159`.
Both are stopped/blocked, not fresh recovery owners. The precise remaining
engineering is: bind a fresh A/B template/spec with joint-boundary recovery,
fresh post-AB paths, then replace the pure-waiting executor using a fresh spec
and runtime while preserving its old receipts and the same canonical publisher.
Do not inject new next_action/gate artifacts at old frozen paths to fool old
consumers. No such executor replacement or recovery start occurred this turn.

## Inode boundary and required platform action

One mount/quota audit identified persistent fuse.autofs
`AutoFS:fsnmafirst1132530`, client total200000 inode/file slots. `/root/autodl-fs`
is the same mount via symlink; alternate names do not create fresh quota.
At11:24CST free94731, below Mut guard100000 by5269, not proven ENOSPC/EDQUOT.
Persistent free bytes1681918603264. NVMe has only about2.85GiB quota-free capacity;
its huge filesystem inode number is not proven user project quota.

No deletion was performed; the already approved safe whitelist was insufficient.
Do not expand deletion to science, checkpoints, input, failed evidence or locks.
Platform request is ready, not sent/purchased:
`/private/tmp/l0-mut-t13-closeout-20260906/PLATFORM_INODE_REQUEST.md`.
It asks for no-restart/no-format online file-count quota verification, suggests
300000 as a planning request, and explains increasing GB may not increase inodes.
Known additional reservations are not complete measured dynamic peaks. Mut
recovery and conditionalRouteB are alternatives, not simultaneous demands.

## T13: real diagnosis, not full training or proven OOM

Immutable code: `ae676fbd04292244df3aee10023c7a5161282e05` at
`/root/autodl-tmp/worktrees/t13-components-ae676fbd`.
Owner324290/start39615932 and science324307/start39616035 are live under GPU1's
existing reservation. Control root:
`control/t13-lazy-repair-20260906/component-owner-303ecdfb-f03d-488d-a85e-cfeaf8c64d99`.
Canonical T13 row was narrowly rebound under the existing publish lock; other
tasks, main matrix, publishers and primary reservation were preserved.

Three eager controls, eager/lazy component evidence, first differences and
fresh matched deterministic diagnostic are implemented. At11:24 target0 reached
`eager_0:epoch0:backward`; comparison list and first_difference remain empty.
The partial receipt's default eager_self_repeatable=false is not proof of
non-determinism before three controls and comparisons complete. No final canary
receipt exists. Max observed process-tree RSS22991450112B; minimum headroom
483720560640B, failcnt4306 unchanged. Do not claim production loss/gradient/RNG
parity or attribute the prior failure to CUDA nondeterminism.
Native exact canary plus existing memory/inode gates may consume the unused
one formal full claim. Deterministic diagnostic is explicitly not automatic
formal approval. Original imported patterns/mining/large transfer were reused.

## T14: retain reference500 and diagnose only the failed stage

Reference500 is complete and preserved. Old low-memory child failed before its
first step because the official non-lead head appends a candidate before the
later graph_map.put. New graph-storev2 reserves only a stable key/ID at append;
it does not insert into the active scientific map early. Actual official AST
dual-head tiny tests preserve graph/actions/RNG/frequencies/order. This is a
physical store repair, not new algorithm or retry3.

Same retry2 replacement bindings were sealed and owner325558 was actually
started on immutable4e524bdc. It then failed before CUDA/science because the
generic GPU wrapper supplied UUID but T14's existing consumer requires numeric
physical index2. The new science output did not exist; there is no checkpoint
resume to claim. The second narrow bootstrap repair39a97e41 is implemented and
tested, integrated as8036e774 and deployed into its own immutable worktree.
Owner326543/start39926973 started with the same replacement/UUID. At03:24:47UTC,
actual Python science326689/start39940357 was verified by argv/cwd, numeric
CUDA_VISIBLE_DEVICES2, GPU2 CUDA520MiB, and new cohort/progress/identity artifacts.
Wrapper326591 is not the actual model process. No new terminal exists. This
proves the bootstrap defect is passed, not510-step parity or formal full PASS.
At03:26:54UTC progress is RUNNING/COHORT_FROZEN,3778 train parents,step0,
calibration/validation/test_loaded=false. This is actual canary science,
not a completed generation stage. Deployment evidence:
`/private/tmp/T14_BOOTSTRAP_DEPLOYMENT_20260906.md`.
Preserve all oldspecs and failure logs; use the same replacement/UUID, not a
second replacement or retry3. LLM's dedicated UUID branch must not be changed.

After both510 paths and all exact comparisons pass, full generation still
requires a fresh same-attempt binding of original master.execution_commit,
authorization.corrected_execution_commit and formal_cadence.execution_commit.
The implemented owner holds explicitly before formal rather than spoofing
those three old pins. No completed reference/GNN work should be replayed.

## T12 and LLM GPU waiting owner

T12 owner162844 and science173495 retain their identities. Latest phase is
REFERENCE_RESUME_TO_500, completed250, not full. At11:24 a10-second window read
139264 bytes and rchar146256 with wchan generic_file_read_iter; positive I/O does not prove
step progress. Complete parity still precedes the existing fresh-zero plan.

LLM final immutable driver:3b2605d681e2ef6116f3cacc662eead2f65dd28a.
Prepared L1/L2/L3 specs, real mandatory GPU finite-forward/generate/save/resume
smoke and common CPU evaluation/publisher commands are sealed at
`outputs/autodl/ablations/llm/core-native-dispatch-3b2605d6-20260906`.
Actual existing owner325928/start39806144 is WAITING_RESOURCE, not GPU science.
It has no GPU lease while waiting. GPU0 remains Mut's primary reservation;
no borrowing/co-location/clearing. GNN/core and main-count gates do not block it;
current blockers are Mut owner/recovery, inode admission, primary reservation
and no observed1200-second unreserved-idle window. Wait is bounded86400 seconds
per generation. A failure/pause ends the current invocation, not unlimited retry.

Native L1 then matched L2(original300-update PPO-LoRA) then2B L3 use the same
attempt contract. L2 must regenerate native-prompt candidates, not adopt the old
plain-prompt pool. No SFT/20B/secondary seeds. Actual GPU smoke is NOT_RUN;
CPU loader/FD/tiny tests are not substituted. Frozen code/model receipts were
reused for driver-only spec rebinding; no repeated model/GNN payload hashes.

## Tests, outputs and exact status commands

LLM resource/smoke adapter23 focused PASS; L0 pipeline26 PASS; Mut18 new plus
47 compatibility PASS; T13 isolated69 plus related21 PASS; T14 integrated53
PASS before its newly diagnosed wrapper correction; that correction57 focused
tests PASS and root's integrated wrapper/LLM regression25 PASS. Counts overlap and are
not summed into a scientific PASS. Root compileall, changed shell wrappers
and git diff check passed. Main user dirty checkout/active execution trees
were not edited. Small code bundles were needed because AutoDL origin points
to a historical offline bundle. Private feature is fast-forward only.

Existing AIDS/BACE PARTIAL outputs remain:
`outputs/autodl/paper_staging/partial-aids-bace-fd98c5f2-20260905`.
Four-dataset final figures remain pending12/16; missing is not zero. L0's result
does not mean all fourLLM variants are complete. GNN remains single-seed7
proposal-fixed sensitivity, not end-to-end or multi-seed robustness.

```bash
/Users/cz0210/miniconda3/envs/smiles_local/bin/python /private/tmp/status_l0_mut_t13_closeout_20260906.py
ssh -o BatchMode=yes tongji-hpc 'sacct -X -j 2560832,2560839,2567257,2567271 --format=JobID,State,ExitCode,Elapsed -P'
ssh -o BatchMode=yes autodl-a800 'cat /autodl-fs/data/counterfactual-subgraph-runtime/control/fast16_matrix_authority/state.json'
ssh -o BatchMode=yes autodl-a800 'cat /autodl-fs/data/counterfactual-subgraph-runtime/control/t13-lazy-repair-20260906/component-owner-303ecdfb-f03d-488d-a85e-cfeaf8c64d99/heartbeat.json'
ssh -o BatchMode=yes autodl-a800 'cat /autodl-fs/data/counterfactual-subgraph-runtime/control/t14_route_c/owners/route-c-59f101cd-f30b-458d-aa8c-2eb93ae82609/heartbeat.json'
ssh -o BatchMode=yes autodl-a800 'cat /autodl-fs/data/counterfactual-subgraph-runtime/outputs/autodl/ablations/llm/core-native-dispatch-3b2605d6-20260906/existing-owner/generation-owner-0/heartbeat.json'
```

Do not run launch commands again for healthy owners. Live status supersedes
this handoff; planned/diagnostic states are never full science acceptance.

Complete requested key=value status and the one bounded final snapshot are in
`/private/tmp/l0-mut-t13-closeout-20260906/final-status-20260906.txt` and
`/private/tmp/l0-mut-t13-closeout-20260906/live_status_1124.json`.
No additional scientific permission is required for the already approved
repairs. Human platform action is needed for the inode/file-quota request;
Mut fresh recovery bindings and T14 post-parity formal bindings are engineering
work, not a reason to ask for the same scientific authorization again.
