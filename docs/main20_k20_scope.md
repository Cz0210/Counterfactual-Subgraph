# 2026-09-10 CM K20 and read-only20-cell paper scope

The user authorizes four datasets/five methods as a paper view referencing the
original16-cell authority plus independent CM publications. No new scientific
authority; BACEv1/v2 and AIDS corrections replace within a cell, not add cells.
Primary K20, K10 supplemental; original RF/GINE family remains separate from GIN/A+.
Original CM deadline2026-09-16T16:27:44Z unchanged. Old generated367 parents and
qualified5474 candidates are reused; post-hoc development after earlier test
inspection is disclosed. No fresh model fit or database acquisition.

Package2666271 failed only at renameat2(RENAME_NOREPLACE), errno22. The prepared
archive is reused. Filesystems lacking the directory operation use exclusive
mkdir and durable data followed by atomic no-replace acceptance receipts; this
is receipt-gated publication, NOT atomic-directory-rename equivalence. Existing
destinations and incomplete outputs remain rejected. Failed evidence is retained.

K20v2 selection is calibration-only, float64, lexicographic coverage/capped sum/
mean frozen-grid coverage, canonical-ID ties, greedy then at most2 strictly
improving best1-out/1-in swaps, retaining the original set as incumbent. Final
set reordered by calibration greedy only inside that set. Test at most20 once.
New datasets require actual original oracle adapters. RF feature-occlusion is
not Grad-CAM, not a learned surrogate, and must reproduce original fingerprints.
IndexRebuilt T13 continuation must retain full optimizer/model/input integrity,
be separately disclosed and pass bounded same-implementation recovery.
