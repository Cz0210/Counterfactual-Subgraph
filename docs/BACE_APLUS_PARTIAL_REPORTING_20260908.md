# A+ partial reporting, 2026-09-08

This offline leaf maps the actual frozen `expanded_pool_new_selector` result
to the common saved-parent reducer. It never selects the best test control.
The unchanged V1 GCFExplainer and ComRecGC rows require the same frozen GIN
weight, temperature and threshold bindings. GlobalGCE-ChemAligned stays PENDING
until its own completed result exists; it is not plotted as zero.

Figure 3 has coverage and original fixed-capped mean cost panels. Figure 4 uses
the exact stored-distance ECDF with K10 primary and K20 auxiliary. Table 2 K10
is reduced from those same parent minima. Conditional median remains a separate
diagnostic; it does not replace capped cost. The observed benchmark and BACE-only
method upgrade are disclosed. No main matrix or experiment registry is changed.

Future GlobalGCE is not permanently hardcoded missing. The optional pair
`--globalgce-results /absolute/copied/results --globalgce-spec /absolute/copied/spec.json`
requires the actual `final_audit.json`, `selection_freeze.json`, `metrics.json`
and a copied `training_contract.json` in that results directory. It validates
the final audit's exact existing state, own freeze/file bindings, unchanged
GIN weights and temperature, common threshold identity and full141 parent
IDs/predictions through the same reducer. Until its final audit exists, no
Global metrics are opened and the method remains PENDING. This option does not
start a task or transport files, and it is not enabled for the current partial.

`replot_bace_gin_aplus.py` produces the source tables from completed metrics and
the actual new freeze. Its optional `--funnel-root` consumes the independent
saved-record funnel, preserving unknown fields rather than inventing zeroes.
The paired CPU-only Slurm wrapper forwards the same arguments. The simpler
`replot_bace_gin.py --display-labels path.json` re-renders already delivered
source CSV without reading science inputs or rewriting supplemental CSV.

The final2659 train reach count combines retained2607 verified reach and five
distinct predeclared uncovered-parent witnesses. The retained theta count 208
is a lower bound, not a full2659 train-distance evaluation. Test only evaluated
the three predeclared controls' rule union, not the complete2659 pool.

Focused regressions cover pending versus zero, no adoption of old Ours metrics,
the predeclared control, frozen-GIN/threshold equality, retained lower-bound
scope, actual funnel preservation, and the common display reducer. Rendering
is not a new model/chemistry execution audit.
